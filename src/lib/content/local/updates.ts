import type { Update } from "../types";
import { placeholderUpdates } from "./placeholders";

// NAMI has supplied no notices, events or news yet; these stand in until it does.
export const updates: readonly Update[] = placeholderUpdates;
